# OPA / Cedar baselines

These policies encode **per-request** authorization as strongly as that model allows without an external residual store.

They are not weakened. Fractionation and session composition require additional shared state. That is the comparison, not "VERITAS is better."
