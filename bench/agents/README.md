# Goal-seeking agents

Scripted adversaries receive goals, not attack recipes. They are not LLMs and they do not judge ALLOW/DENY.

```bash
veritas lab agents
```

A local model can later drive the same harness. The deterministic engine remains the authority.
