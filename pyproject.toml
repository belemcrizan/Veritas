[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "veritas-boundary-poc"
version = "0.2.0"
description = "A deterministic execution control plane prototype for trajectory-aware authorization of autonomous agents"
readme = "README.md"
requires-python = ">=3.12"
authors = [{name = "Crizan Belem Ribeiro"}]
keywords = ["agentic-ai", "authorization", "capabilities", "security", "formal-methods"]
license = "LicenseRef-Proprietary"
license-files = ["LICENSE-PROVISIONAL.md"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed",
]
dependencies = [
  "cryptography>=43,<47",
  "pydantic>=2.8,<3",
]

[project.optional-dependencies]
dev = [
  "hypothesis>=6.112,<7",
  "mypy>=1.11,<2",
  "pytest>=8.3,<9",
  "ruff>=0.6,<1",
  "z3-solver>=4.13,<5",
]
docs = [
  "mkdocs>=1.6,<2",
  "mkdocstrings[python]>=0.27,<1",
]
postgres = [
  "psycopg[binary]>=3.2,<4",
]
redis = [
  "redis>=5,<6",
]
release = [
  "build>=1.2,<2",
  "twine>=5.1,<7",
]

[project.urls]
Repository = "https://github.com/belemcrizan/Veritas"
Documentation = "https://github.com/belemcrizan/Veritas/tree/main/docs"
Issues = "https://github.com/belemcrizan/Veritas/issues"

[project.scripts]
veritas = "veritas.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
veritas = ["py.typed"]
"veritas.resources" = ["*.json"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "S"]
ignore = ["S101"]

[tool.ruff.lint.per-file-ignores]
"src/veritas/reasons.py" = ["E501"]
"src/veritas/comparison.py" = ["E501", "S106"]
"src/veritas/baselines.py" = ["E501"]
"src/veritas/demo.py" = ["E501"]
"src/veritas/showcase.py" = ["E501"]
"src/veritas/lab.py" = ["E501", "S110", "S607"]
"src/veritas/workloads.py" = ["E501", "S607"]
"src/veritas/adapters/postgres.py" = ["E501"]
"src/veritas/bench_cycle2.py" = ["E501"]
"src/veritas/science.py" = ["E501"]
"src/veritas/cli.py" = ["E501"]
"src/veritas/agents.py" = ["E501"]
"src/veritas/baselines_ext.py" = ["E501"]
"tests/test_crash.py" = ["S603"]
"tests/test_cycle2.py" = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["veritas"]

[[tool.mypy.overrides]]
module = ["psycopg", "psycopg.*", "redis", "redis.*"]
ignore_missing_imports = true
